# Semi-mpc-zerotest

## How to run
Activate googletest ```TEST(ZeroTest, zerotest)``` to start ```zerotest```.
```
git clone https://github.com/mora-2/semi-mpc-zerotest.git
cd semi-mpc-zerotest/test
mkdir build
cd build
cmake ..
make
./gTest
```
